; 动捕yaw轴矫正
; 加入MOVE指令，输入相对位置坐标：K210固件，上位机，Python
; TCP发送的调试数据包如果不被接收的话会导致收数据延迟，需要解决
; 调参功能，
;     参数保存功能，
;     IMU零漂校准,
;     上位机指定飞机ID（IP地址）
;     是否发送调试数据
指令摇篮复飞好像有点问题
; TCP->UDP crc （esp固件）
;     esp UDPserver 
;     k210增加使用UDP指令 crc校验s
    
安全保护逻辑
; 上位机串口连接 修改网络参数
; 修改TAKE_OFF状态控制，包括电机加速到悬停10厘米，加速至起飞油门后开始运行角度环和高度速度环，而非直接运行高度环